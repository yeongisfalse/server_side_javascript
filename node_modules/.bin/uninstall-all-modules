#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../uninstall-all-modules/uninstall-all-modules" "$@"
  ret=$?
else 
  node  "$basedir/../uninstall-all-modules/uninstall-all-modules" "$@"
  ret=$?
fi
exit $ret
